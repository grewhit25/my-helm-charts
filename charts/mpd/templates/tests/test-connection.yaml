apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mpd.fullname" . }}-test-connection"
  labels:
{{ include "mpd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "mpd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
