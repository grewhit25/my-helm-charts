apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "snapcast-server.fullname" . }}-test-connection"
  labels:
{{ include "snapcast-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  #containers:
  #  - name: wget
  #    image: busybox
  #    command: ['wget']
  #    args:  ['{{ include "snapcast-server.fullname" . }}:{{ .Values.ports.server.port }}']
  restartPolicy: Never
