apiVersion: v1
kind: Service
metadata:
  name: {{ include "snapcast-client.fullname" . }}
  labels:
    {{- include "snapcast-client.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "snapcast-client.selectorLabels" . | nindent 4 }}
