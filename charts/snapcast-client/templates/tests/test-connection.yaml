apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "snapcast-client.fullname" . }}-test-connection"
  labels:
{{ include "snapcast-client.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "snapcast-client.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
